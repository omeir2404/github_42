1.ask how to fix the no space error when printing the stack

2. ask if theres an easier way of checking if the args surpass the int limits

			// if (arg[i][0] == '+' || arg[i][0] == '-')
			// 	digit++;